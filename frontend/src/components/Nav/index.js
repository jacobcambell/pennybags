import styles from './Nav.module.css';

const Nav = () => {
    return (
        <div className={styles.nav}>
            <p className={styles.logo}>iMonopoly</p>
        </div>
     );
}

export default Nav;